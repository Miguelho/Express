extends layout

block content
  h1= title
  p Welcome to #{title}
  p User: #{user}
  if(!user)
    a(href='/login') Login
    br
    a(href='/register') Register
  if(user)
    p Yout are currently logged in as #{user.username}
    a(href='/logout') Logout