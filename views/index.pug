extends layout

block content
  div(ng-controller='IndexController')
  h1 Bienvenido a GeoData
  p aqui estamos testeando
  if(!user)
    a(href='/login') Login
    br
    a(href='/register') Register
  if(user)
    p Yout are currently logged in as #{user.username}
    a(href='/logout') Logout
